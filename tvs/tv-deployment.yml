apiVersion: v1
kind: ConfigMap
metadata:
  name: tv-config
data:
  CATEGORY: "TV & Appliances"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tv-deployment
  labels:
    app: tv
spec:
  replicas: 2
  selector:
    matchLabels:
      app: tv
  template:
    metadata:
      labels:
        app: tv
    spec:
      containers:
      - name: tv
        image: your-registry/ecom-tv:latest  # Replace with your Docker image
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "200m"
        envFrom:
        - configMapRef:
            name: tv-config
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: tv-service
spec:
  selector:
    app: tv
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP